#include <iostream>
using namespace std;

int main() {
    int n, m, h, tmp;
    cin >> n >> m >> h;
    for (int i = 0; i < n; i++) {
        cin >> tmp;
        tmp* tmp <= m * m + h * h ? cout << "DA\n" : cout << "NE\n";
    }
}